from itertools import count

id_usuario_role = count(1)
usuarios_roles = []

id_usuario = int(input("ID do usuário: "))
id_role = int(input("ID do role: "))

usuarios_roles.append({
    "id": next(id_usuario_role),
    "id_usuario": id_usuario,
    "id_role": id_role
})
print(usuarios_roles)
