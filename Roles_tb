from itertools import count

id_role = count(1)
roles = []

nome = input("Nome do role: ")
descricao = input("Descrição: ")

roles.append({
    "id": next(id_role),
    "nome": nome,
    "descricao": descricao
})
print(roles)
